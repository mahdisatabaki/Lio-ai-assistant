# Environment contract from docs/TECH.md section 34.
# Copy to .env.local and fill in real values. Never commit a filled-in file.

# Liara PostgreSQL DBaaS connection string (server only)
DATABASE_URL=

# Liara AI API (server only)
LIARA_AI_API_KEY=
LIARA_AI_BASE_URL=
LIARA_CHAT_MODEL=
LIARA_EMBEDDING_MODEL=
